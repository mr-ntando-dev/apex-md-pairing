# ============================================================
#  APEX-MD Pairing Site  ·  Environment Variables
#  Render sets PORT automatically — do not override it there.
# ============================================================

# PORT is injected by Render automatically
# PORT=3000

# Optional: custom session directory (default: ./session)
# SESSION_DIR=./session
